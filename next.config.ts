import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Bilder von beliebigen Quellen erlauben (wir nutzen normale <img>-Tags,
  // deshalb ist hier aktuell nichts weiter nötig).
};

export default nextConfig;
